# OpenOCD configuration for ESP32-S3
source [find interface/esp_usb_jtag.cfg]
source [find target/esp32s3.cfg]

adapter speed 40000

# Reset configuration
reset_config none

# Enable semi-hosting if needed
#arm semihosting enable